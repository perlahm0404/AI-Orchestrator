# Browser Automation Package - Environment Variables

# REQUIRED: Encryption key for credential vault (AES-256-GCM)
# Generate with: openssl rand -hex 32
BROWSER_AUTOMATION_KEY=your-32-byte-hex-key-here

# OPTIONAL: Claude API key for LLM-powered data extraction
# Only needed if using data-extractor module
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# OPTIONAL: Audit log directory
# Defaults to ./audit-logs if not specified
AUDIT_LOG_PATH=./audit-logs

# OPTIONAL: Screenshot directory for evidence capture
# Defaults to ./screenshots if not specified
SCREENSHOT_PATH=./screenshots

# OPTIONAL: Session timeout in milliseconds
# Defaults to 1800000 (30 minutes)
SESSION_TIMEOUT_MS=1800000

# OPTIONAL: Browser headless mode
# Set to false for debugging (shows browser window)
HEADLESS=true
