# AI Orchestrator Environment Variables

# Gmail API OAuth Credentials (optional - for email labeling feature)
# Get credentials from: https://console.cloud.google.com
# 1. Create a new project or select existing project
# 2. Enable Gmail API for the project
# 3. Create OAuth 2.0 Client ID (Desktop app type)
# 4. Download credentials JSON file
# 5. Set path to credentials file here:
# GMAIL_OAUTH_CREDENTIALS=/path/to/credentials.json
